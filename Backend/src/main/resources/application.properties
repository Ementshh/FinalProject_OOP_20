spring.application.name=LabuBoom-BE

# Database Configuration
# Uses environment variable SPRING_DATASOURCE_URL if available, otherwise uses the default Neon URL
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-flat-breeze-a18cv38v-pooler.ap-southeast-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_FE5JOxRWl8up&sslmode=require&channelBinding=require}

# If you need to separate username/password in other environments:
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_FE5JOxRWl8up}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Port
# Render/Cloud platforms usually provide a PORT env var
server.port=${PORT:8080}

# Debug logging
logging.level.org.springframework.web=DEBUG
logging.level.com.labubushooter.backend=DEBUG